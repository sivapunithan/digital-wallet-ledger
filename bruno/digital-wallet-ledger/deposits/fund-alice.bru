meta {
  name: Fund Alice
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/deposits
  body: json
  auth: none
}

body:json {
  {
    "toAccountId": "{{aliceId}}",
    "amount": 500.00,
    "idempotencyKey": "{{$randomUUID}}"
  }
}